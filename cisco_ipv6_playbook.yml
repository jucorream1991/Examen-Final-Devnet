---
- name: Configurar Interfaz Loopback 33 con IPv6 via Ansible
  hosts: routers
  gather_facts: no
  connection: network_cli

  tasks:
    - name: Configurar IPv6 global y link-local en Loopback 33
      ios_config:
        lines:
          - interface Loopback33
          - description Creada via Ansible Playbook - Item 6
          - ipv6 address 3001:ABCD:ABCD:1::1/128
          - ipv6 address FE80::1 link-local
          - no shutdown